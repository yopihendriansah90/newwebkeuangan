@extends('layout')
@section('content')
<div class="page-heading"><div><p class="eyebrow">PENGATURAN DATA</p><h1>Kategori</h1><p class="muted">Rapikan transaksi dengan kategori dan ikon yang mudah dikenali.</p></div><button data-modal="categoryModal" class="button primary">＋ Tambah kategori</button></div>
<div class="category-columns"><section class="panel"><div class="panel-head"><h2>Pemasukan</h2><span class="count green-bg">{{ $categories->where('type', 'income')->count() }}</span></div>@include('partials.category-list', ['type' => 'income'])</section><section class="panel"><div class="panel-head"><h2>Pengeluaran</h2><span class="count red-bg">{{ $categories->where('type', 'expense')->count() }}</span></div>@include('partials.category-list', ['type' => 'expense'])</section></div>
<dialog id="categoryModal"><div class="modal-head"><div><p class="eyebrow">KATEGORI</p><h2>Tambah kategori</h2></div><button class="close icon-button">×</button></div><form method="post" action="{{ route('categories.store') }}" class="form-stack">@csrf<label>Nama kategori<input name="name" required placeholder="Contoh: Belanja"></label><label>Jenis<div class="modern-select" data-select data-name="category_type"><input type="hidden" name="type" value="expense" required><button type="button" class="modern-select-trigger"><span data-select-label>Pengeluaran</span><span class="select-chevron">⌄</span></button><div class="modern-select-menu"><button type="button" data-value="income">Pemasukan<span>Uang yang diterima</span></button><button type="button" data-value="expense">Pengeluaran<span>Uang yang digunakan</span></button></div></div></label><label>Ikon kategori<div class="icon-picker">@foreach(config('category_icons') as $icon => $label)<label title="{{ $label }}"><input type="radio" name="icon" value="{{ $icon }}" @checked($icon === 'more_horiz')><span class="material-symbols-rounded">{{ $icon }}</span></label>@endforeach</div></label><button class="button primary wide">Simpan kategori</button></form></dialog>
@endsection
