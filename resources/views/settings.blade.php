@extends('layout') @section('content')<div class="page-heading"><div><p class="eyebrow">KONFIGURASI SISTEM</p><h1>Pengaturan</h1><p class="muted">Kelola integrasi bot dan lakukan pengujian tanpa terminal.</p></div></div><section class="panel credentials-panel"><div class="panel-head"><div><h2>Kredensial API</h2><p class="muted">Nilai disimpan terenkripsi di database.</p></div><span class="status-dot online">Aman</span></div><form method="post" action="{{ route('settings.credentials') }}" class="credentials-form">@csrf<label>Telegram Bot Token<input type="password" name="telegram_bot_token" placeholder="Masukkan token dari BotFather" autocomplete="new-password"></label><label>Telegram Webhook Secret<input type="password" name="telegram_webhook_secret" placeholder="Secret untuk validasi webhook" autocomplete="new-password"></label><label>Groq API Key<input type="password" name="groq_api_key" placeholder="Masukkan API key Groq" autocomplete="new-password"></label><label>Groq Model<input name="groq_model" value="{{ config('services.groq.model') }}" placeholder="llama-3.3-70b-versatile"></label><div class="credential-note">Kosongkan field rahasia jika tidak ingin mengubah nilai yang sudah tersimpan.</div><button class="button primary">Simpan kredensial</button></form></section><div class="settings-grid"><section class="panel settings-card"><div class="settings-card-head"><div><span class="settings-icon telegram-settings">✈</span><h2>Telegram Bot</h2><p class="muted">Webhook dan koneksi bot</p></div><span class="status-dot {{ $telegramConfigured?'online':'offline' }}">{{ $telegramConfigured?'Ter konfigurasi':'Belum diatur' }}</span></div><div class="settings-list"><div><span>Bot token</span><strong>{{ $telegramConfigured?'••••••••••••':'Belum tersedia' }}</strong></div><div><span>Webhook</span><strong>{{ !empty($webhook['url'])?'Aktif':'Belum tersinkron' }}</strong></div>@if(!empty($webhook['url']))<div><span>URL aktif</span><strong class="settings-url">{{ $webhook['url'] }}</strong></div>@endif @if(!empty($webhook['last_error_message']))<div class="settings-error"><span>Error terakhir</span><strong>{{ $webhook['last_error_message'] }}</strong></div>@endif</div><div class="settings-actions"><form method="post" action="{{ route('settings.telegram.test') }}">@csrf<button class="button light-button">Tes koneksi</button></form><form method="post" action="{{ route('settings.telegram.sync') }}">@csrf<button class="button primary">Sinkronkan webhook</button></form><form method="post" action="{{ route('settings.telegram.remove') }}">@csrf<button class="button danger-button">Hapus webhook</button></form></div></section><section class="panel settings-card"><div class="settings-card-head"><div><span class="settings-icon ai-settings">✦</span><h2>Groq AI</h2><p class="muted">Parser bahasa natural transaksi</p></div><span class="status-dot {{ $groqConfigured?'online':'offline' }}">{{ $groqConfigured?'Ter konfigurasi':'Belum diatur' }}</span></div><div class="settings-list"><div><span>API key</span><strong>{{ $groqConfigured?'••••••••••••':'Belum tersedia' }}</strong></div><div><span>Model</span><strong>{{ config('services.groq.model') }}</strong></div></div><form method="post" action="{{ route('settings.groq.test') }}" class="groq-test-form">@csrf<label>Pesan pengujian<textarea name="text" rows="3" placeholder="Contoh: makan 10k kemarin">{{ old('text','makan 10k kemarin') }}</textarea></label><button class="button primary">Tes parsing Groq</button></form>@if(session('groq_test'))<div class="ai-result"><small>Hasil respons AI</small><p>{{ session('groq_test') }}</p></div>@endif</section></div><section class="panel settings-help"><div><p class="eyebrow">STATUS KEAMANAN</p><h2>Konfigurasi terenkripsi</h2><p class="muted">Token dan API key tidak ditampilkan penuh di halaman ini.</p></div><code>TELEGRAM_BOT_TOKEN<br>TELEGRAM_WEBHOOK_SECRET<br>GROQ_API_KEY</code></section>@endsection
