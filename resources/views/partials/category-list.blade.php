@forelse($categories->where('type',$type) as $category)<div class="category-row"><span class="category-dot" style="background:{{ $category->color }}"></span><div><strong>{{ $category->name }}</strong><small>{{ $category->is_active?'Aktif':'Nonaktif' }}</small></div><div class="category-actions"><button data-modal="edit-{{ $category->id }}" class="icon-button">✎</button><form method="post" action="{{ route('categories.destroy',$category) }}">@csrf @method('DELETE')<button class="icon-button">×</button></form></div></div><dialog id="edit-{{ $category->id }}"><div class="modal-head"><h2>Edit kategori</h2><button class="close icon-button">×</button></div><form method="post" action="{{ route('categories.update',$category) }}" class="form-stack">@csrf @method('PATCH')<label>Nama kategori<input name="name" value="{{ $category->name }}" required></label><button class="button primary wide">Simpan perubahan</button></form></dialog>@empty<p class="empty-copy">Belum ada kategori.</p>@endforelse
