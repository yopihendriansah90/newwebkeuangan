@extends('layout') @section('content')<div class="page-heading"><div><p class="eyebrow">INTEGRASI TELEGRAM</p><h1>Telegram</h1><p class="muted">Hubungkan beberapa akun Telegram ke {{ $wallet->name }}.</p></div><form method="post" action="{{ route('telegram.pairing-code') }}">@csrf<button class="button primary">＋ Buat kode pairing</button></form></div>@if(session('pairing_code'))<div class="pairing-card"><div><p class="eyebrow">KODE PAIRING ANDA</p><h2 data-pairing-code>{{ session('pairing_code') }}</h2><p class="muted">Kirim ke bot Telegram: <strong>/hubungkan {{ session('pairing_code') }}</strong></p><small>Kode berlaku selama 10 menit.</small><div class="pairing-actions"><button type="button" class="button light-button" data-copy-pairing>Salin kode</button><button type="button" class="button primary" data-share-telegram>Bagikan ke Telegram</button></div></div><span class="pairing-icon">↗</span></div>@endif<div class="panel members-panel"><div class="panel-head"><h2>Telegram terhubung</h2><span class="count green-bg">{{ $connections->count() }}</span></div><div class="members-table-wrap"><table class="members-table"><thead><tr><th>No</th><th>Akun Telegram</th><th>Pemilik akun</th><th>Terhubung</th><th>Aksi</th></tr></thead><tbody>@forelse($connections as $connection)<tr><td class="row-number">{{ sprintf('%02d',$loop->iteration) }}</td><td><div class="member-cell"><span class="member-avatar">✈</span><strong>{{ $connection->telegram_username ? '@'.$connection->telegram_username : ($connection->telegram_name ?? $connection->chat_id) }}</strong></div></td><td class="muted">{{ $connection->user->name }}</td><td class="muted">{{ optional($connection->connected_at)->format('d M Y H:i') ?? '—' }}</td><td><form method="post" action="{{ route('telegram.disconnect',$connection) }}">@csrf @method('DELETE')<button class="icon-button" title="Putuskan">×</button></form></td></tr>@empty<tr><td colspan="5" class="empty-copy">Belum ada akun Telegram yang terhubung.</td></tr>@endforelse</tbody></table></div></div>@endsection
