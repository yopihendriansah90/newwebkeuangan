@extends('layout') @section('content')<div class="page-heading"><div><p class="eyebrow">DOMPET BERSAMA</p><h1>Anggota</h1><p class="muted">{{ $wallet->name }} · {{ $members->count() }} anggota</p></div><button data-modal="memberModal" class="button primary">＋ Tambah anggota</button></div><div class="panel members-panel"><div class="panel-head"><h2>Akses dompet</h2><span class="count red-bg">{{ $members->count() }}</span></div><div class="members-table-wrap"><table class="members-table"><thead><tr><th>No</th><th>Anggota</th><th>Username</th><th>Peran</th><th>Aksi</th></tr></thead><tbody>@foreach($members as $member)<tr><td class="row-number">{{ sprintf('%02d',$loop->iteration) }}</td><td><div class="member-cell"><span class="member-avatar">{{ strtoupper(substr($member->username,0,1)) }}</span><strong>{{ $member->name }}</strong></div></td><td class="muted">{{ '@'.$member->username }}</td><td><span class="role-badge {{ $member->pivot->role==='owner'?'owner':'' }}">{{ $member->pivot->role==='owner'?'Pemilik':'Anggota' }}</span></td><td>@if($member->id!==auth()->id() && $members->first(fn($item)=>$item->id===auth()->id())?->pivot->role==='owner')<form method="post" action="{{ route('members.destroy',$member) }}">@csrf @method('DELETE')<button class="icon-button" title="Hapus akses">×</button></form>@else<span class="muted">—</span>@endif</td></tr>@endforeach</tbody></table></div></div><dialog id="memberModal"><div class="modal-head"><div><p class="eyebrow">DOMPET BERSAMA</p><h2>Tambah anggota</h2></div><button class="close icon-button">×</button></div><form method="post" action="{{ route('members.store') }}" class="form-stack">@csrf<label>Nama lengkap<input name="name" placeholder="Contoh: Istri" required></label><label>Username<input name="username" placeholder="Contoh: istri" required></label><label>Password<input type="password" name="password" minlength="6" required></label><label>Konfirmasi password<input type="password" name="password_confirmation" minlength="6" required></label><button class="button primary wide">Buat akun anggota</button></form></dialog>@endsection
